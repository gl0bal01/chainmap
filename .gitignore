# Dependencies (dev-only; the app runs with zero install)
node_modules/

# Orchestration / tooling artifacts (not part of the app)
.omc/
.omo/
.codegraph
.claude/
# Implementation plans are process artifacts (TDD step lists, agent workflow
# headers) — kept local, not in the public repo. Design specs under
# docs/superpowers/specs/ ARE tracked (product rationale).
docs/superpowers/plans/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / temp
*.log
